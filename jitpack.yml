jdk:
  - openjdk17
before_install:
  - wget -q https://services.gradle.org/distributions/gradle-8.9-bin.zip -O /tmp/gradle.zip
  - unzip -q /tmp/gradle.zip -d $HOME
install:
  - $HOME/gradle-8.9/bin/gradle -p android clean build publishToMavenLocal -x test
